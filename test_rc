 Based on my analysis, here are some practical curl examples for controlling your Sony Bravia
   TV through the gateway:

  📡 Gateway TV Remote Control - Curl Examples

  API Endpoint Format

  POST /api/users/{user_id}/devices/{device_id}/action

  Basic Remote Control Commands

  1. Power Control

  # Power toggle
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "power",
      "parameters": {}
    }'

  # Power on
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "power_on",
      "parameters": {}
    }'

  # Power off
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "power_off",
      "parameters": {}
    }'

  2. Volume Control

  # Volume up
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "volume_up",
      "parameters": {}
    }'

  # Volume down
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "volume_down",
      "parameters": {}
    }'

  # Mute toggle
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "mute",
      "parameters": {}
    }'

  3. Navigation Control

  # Navigate up
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "up",
      "parameters": {}
    }'

  # Navigate down
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "down",
      "parameters": {}
    }'

  # Navigate left
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "left",
      "parameters": {}
    }'

  # Navigate right
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "right",
      "parameters": {}
    }'

  # Confirm/OK button
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "confirm",
      "parameters": {}
    }'

  4. Menu & Input Control

  # Home button
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "home",
      "parameters": {}
    }'

  # Menu button
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "menu",
      "parameters": {}
    }'

  # Switch to HDMI 1
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "hdmi1",
      "parameters": {}
    }'

  # Input source button
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "input",
      "parameters": {}
    }'

  5. Channel Control

  # Channel up
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "channel_up",
      "parameters": {}
    }'

  # Channel down
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "remote",
      "action": "channel_down",
      "parameters": {}
    }'

  Advanced Control API Commands

  6. System Information

  # Get power status
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "control",
      "action": "power_status",
      "parameters": {}
    }'

  # Get system info
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "control",
      "action": "system_info",
      "parameters": {}
    }'

  # Get volume info
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "control",
      "action": "volume_info",
      "parameters": {}
    }'

  # Get currently playing content
  curl -X POST "http://localhost:8080/api/users/1/devices/living_room_tv/action" \
    -H "Content-Type: application/json" \
    -d '{
      "type": "control",
      "action": "playing_content",
      "parameters": {}
    }'

  Quick Test Script

  #!/bin/bash
  # tv_remote.sh - Quick TV control script
  GATEWAY_URL="http://localhost:8080"
  USER_ID="1"
  DEVICE_ID="living_room_tv"

  case "$1" in
    "on")     ACTION="power_on" ;;
    "off")    ACTION="power_off" ;;
    "vol+")   ACTION="volume_up" ;;
    "vol-")   ACTION="volume_down" ;;
    "mute")   ACTION="mute" ;;
    "hdmi1")  ACTION="hdmi1" ;;
    "home")   ACTION="home" ;;
    *)        echo "Usage: $0 {on|off|vol+|vol-|mute|hdmi1|home}"; exit 1 ;;
  esac

  curl -X POST "${GATEWAY_URL}/api/users/${USER_ID}/devices/${DEVICE_ID}/action" \
    -H "Content-Type: application/json" \
    -d "{\"type\": \"remote\", \"action\": \"${ACTION}\", \"parameters\": {}}"

  Usage:

  chmod +x tv_remote.sh
  ./tv_remote.sh on      # Turn TV on
  ./tv_remote.sh vol+    # Volume up
  ./tv_remote.sh hdmi1   # Switch to HDMI 1

  Notes:

  - Replace localhost:8080 with your gateway's actual IP/domain
  - user_id: 1 - You may need to adjust based on your user setup
  - device_id: living_room_tv - Matches your hub.yml configuration
  - All commands return JSON responses with success status and any returned data

  These curl commands will send instructions through your gateway over the internet to your
  home hub, which will then control your Sony Bravia TV locally! 🎮📺
